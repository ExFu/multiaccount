# Node
node_modules/
npm-debug.log
yarn-error.log

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
env/

# Build
dist/
build/

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Credentials and tokens
*.token.json
client_secret*.json
credentials.json
.credentials
token_cache/
# agent-plan-visualiser local per-checkout state — never tracked
.apv/.last-capture
# agent-plan-visualiser local per-checkout state — never tracked
.apv/.pending-extract
# agent-plan-visualiser local per-checkout state — never tracked
.apv/.toolchain-home
# agent-plan-visualiser derived artefacts — rebuilt, never tracked
.apv/cache.sqlite
.apv/projection.json
.apv/summary.md
.apv/bin/
